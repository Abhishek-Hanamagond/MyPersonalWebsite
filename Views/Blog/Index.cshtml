@model IEnumerable<MyPersonalWebsite.Models.Blog>

@{
    ViewData["Title"] = "Blogs";
}

<h1>Blogs</h1>

@* <!-- Search form -->
<form class="d-flex" method="get" asp-controller="Blog" asp-action="Search">
    <input class="form-control me-2" type="search" placeholder="Search blogs..." name="query" aria-label="Search">
    <button class="btn btn-outline-primary" type="submit">Search</button>
</form> *@
<br/>

<div class="row">
    @foreach (var item in Model) {
        <div class="col-md-4 fade-in">
            <div class="product-card">
                <div class="card-body">
                    <h5 class="card-title">@Html.DisplayFor(modelItem => item.Title)</h5>
                    <p class="card-text">@Html.DisplayFor(modelItem => item.Content)</p>
                    <p class="card-text"><small class="text-muted">@Html.DisplayFor(modelItem => item.CreatedAt)</small></p>
                </div>
            </div>
        </div>
    }
</div>
